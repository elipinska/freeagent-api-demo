<h2>Your OAuth tokens are safe with us 👌.</h2>
<p>Your access token will expire in <%= distance_of_time_in_words(Time.zone.now, @freeagent_api_authentication.expires_at) %>.</p>
<%= form_with(model: @freeagent_api_authentication, local: true) do |form| %>
  <% if @freeagent_api_authentication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@freeagent_api_authentication.errors.count, "error") %> prohibited this authentication from being saved:</h2>

      <ul>
      <% @freeagent_api_authentication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions">
    <%= form.submit "Renew access token", class:"btn", data: { confirm: "Are you sure?" } %>
  </div>
<% end %>
<br>
<h3>Now take a look at some of the cool things you can do with the FreeAgent API.</h3>
<%= link_to "FreeAgent API requests", freeagent_api_requests_path, class: "btn" %>
